diff --git a/cmake/DownloadAndExtractClang.cmake b/cmake/DownloadAndExtractClang.cmake
index 08d07f70f54a021a4a904883a332589187c06da5..2a6e9be974d0ededa552232ac810ccd0d61c2f88 100644
--- a/cmake/DownloadAndExtractClang.cmake
+++ b/cmake/DownloadAndExtractClang.cmake
@@ -26,6 +26,9 @@ if(${CMAKE_SYSTEM_NAME} STREQUAL Linux)
     if(${UBUNTU_VERSION} MATCHES 16.04)
       set(CLANG_ARCHIVE_NAME 
           clang+llvm-${CLANG_VERSION}-x86_64-linux-gnu-ubuntu-16.04)
+    elseif(${UBUNTU_VERSION} MATCHES 18.04)
+      set(CLANG_ARCHIVE_NAME
+          clang+llvm-${CLANG_VERSION}-x86_64-linux-gnu-ubuntu-16.04)
     endif()
   endif()
 
